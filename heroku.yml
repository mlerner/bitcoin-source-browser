build:
    docker:
        web: docker/debian/Dockerfile
    config:
        GIT_REPO_URL: https://github.com/bitcoin/bitcoin.git
        PROJECT: bitcoin
run:
    web: /usr/sbin/apache2ctl -D FOREGROUND
